{
  "mcpServers": {
    "ontoderive": {
      "command": "python3",
      "args": ["${workspaceFolder}/engine/mcp-server.py"],
      "description": "OntoDerive 推导引擎 — 事实驱动方案推导",
      "env": {"PYTHONPATH": "${workspaceFolder}/engine"}
    }
  }
}
